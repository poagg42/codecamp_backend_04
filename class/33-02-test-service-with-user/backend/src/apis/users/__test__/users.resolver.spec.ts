describe('UsersResolver', () => {
  beforeEach(() => {});

  describe('fetchUser', () => {
    it('유저정보 꺼내오기', () => {
      // 컨트롤러와 같은 방식으로 테스트하기!!
    });
  });

  describe('createUser', () => {
    it('유저 등록하기', () => {
      // 컨트롤러와 같은 방식으로 테스트하기!!
    });
  });
});
